# 1. 파이썬 컴파일 파일 (불필요)
__pycache__/
*.pyc

# 2. 가상환경 폴더 (용량이 커서 Git에 올리면 안 됨)
rag_env/
venv/
env/
.venv/

# 3. VS Code 설정 폴더 (내 컴퓨터 설정이 남에게 가면 안 됨)
.vscode/

# 4. 환경변수/비밀번호 파일 (절대 올리면 안 됨!!!)
.env
keys.json